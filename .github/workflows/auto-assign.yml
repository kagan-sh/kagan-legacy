name: Auto-assign

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  issues: write
  pull-requests: write

jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const assignee = 'kagan-agent';
            if (context.eventName === 'issues') {
              await github.rest.issues.addAssignees({
                ...context.repo,
                issue_number: context.issue.number,
                assignees: [assignee],
              });
            } else {
              await github.rest.issues.addAssignees({
                ...context.repo,
                issue_number: context.payload.pull_request.number,
                assignees: [assignee],
              });
            }
